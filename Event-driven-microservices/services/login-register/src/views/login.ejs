<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Howlite Shuttle</title>
    <link rel="stylesheet" href="/css/output.css">
    <link rel="stylesheet" href="/css/custom.css"> <!-- Include custom CSS here -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>
<body class="font-poppins bg-slate-50">
    <div class="">
        <%- include('navbar') %>   
    </div>
    

    <!-- Main content here -->
    <section class="mt-24">
        <% if (message) { %>
            <div class="bg-green-100 border border-green-400 text-green-700 mb-4 px-4 py-3 rounded relative text-center max-w-lg mx-auto" role="alert">
                <span class="block sm:inline"><%= message %></span>
            </div>
        <% } %>
      <div class="flex justify-center items-center">
        <div class="max-w-4xl bg-white shadow-md rounded-lg p-8 flex">
          <div class="hidden lg:flex lg:w-1/3 mr-8 items-center">
            <div class="w-64 h-64 overflow-hidden rounded-lg shadow-md">
                <img src="/images/bus_hero_image.jpg" alt="Login Image" class="object-cover min-w-full min-h-full">
            </div>
          </div>
          <div class="lg:w-2/3">
            <h2 class="text-2xl font-semibold text-center text-gray-800">Login</h2>
            <form action="/login" method="POST">
                <div class="mt-4">
                    <label for="email" class="block text-sm font-medium text-gray-700">Email</label>
                    <input type="email" id="email" name="email" required class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm">
                </div>
                <div class="mt-4">
                    <label for="password" class="block text-sm font-medium text-gray-700">Password</label>
                    <input type="password" id="password" name="password" required class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm">
                </div>
                <div class="mt-6">
                    <button type="submit" class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-customBlue-base hover:bg-customBlue-deep focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-customBlue-deep transition-colors duration-300 ease-in-out">
                        Sign In
                    </button>
                </div>
            </form>
          </div>
        </div>
      </div>
      </div>
        </form>
    </section>
    
    <div class="mt-40"> <!-- Added margin to create space between main content and footer -->
        <%- include('footer') %>
    </div>

</body>
</html>