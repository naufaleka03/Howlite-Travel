<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Howlite Shuttle</title>
    <link rel="stylesheet" href="/css/output.css">
    <link rel="stylesheet" href="/css/custom.css"> <!-- Include custom CSS here -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>
<body class="font-poppins">
    <div class=""> <!-- Add padding to the container -->
        <%- include('navbar') %>   
    </div>
    
    <!-- Main content here -->
    <div class="relative pb-[450px] bg-slate-100"> <!-- Add padding to the container -->
        <img src="/images/bus_hero_image.jpg" alt="Hero Image" class="w-full h-80 object-cover brightness-75">
        <div class="flex gap-8 ml-20 absolute top-20 left-0 right-0"> <!-- Group sidebar and booking list in a flex container with a gap, overlaying the hero image -->
            <%- include('sidebar') %>
            <div class="w-2/3 p-6 bg-slate-50 rounded-lg shadow-md bg-opacity-95 mb-6" alt="Profile Setting">
                <div class="flex justify-between items-center">
                    <div>
                        <h1 class="font-semibold">Pengaturan Profile</h1>
                        <h2 class="text-sm text-gray-400">Lakukan penyesuaian data profile anda</h2>
                    </div>
                </div>
                <hr class="m-6 w-full border-1 border-gray-300 mx-auto"/>
                <div class="profile-settings bg-white shadow-md p-6 rounded-lg">
                    <form action="/profile/<%= user.user_id %>/update" method="POST" enctype="application/x-www-form-urlencoded">
                        <!-- Form Fields -->
                        <div class="mb-4">
                            <label for="username" class="block text-sm font-medium text-gray-700">Username</label>
                            <input type="text" id="username" name="username" value="<%= user.username %>" class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm" required>
                        </div>
                        <div class="mb-4">
                            <label for="email" class="block text-sm font-medium text-gray-700">Email</label>
                            <input type="email" id="email" name="email" value="<%= user.email %>" class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm" required>
                        </div>
                        <div class="mb-4">
                            <label for="phone" class="block text-sm font-medium text-gray-700">Phone Number</label>
                            <input type="tel" id="phone" name="phone" value="<%= user.phone %>" class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm" required>
                        </div>
                        <div class="mb-4">
                            <label for="gender" class="block text-sm font-medium text-gray-700">Jenis Kelamin (Laki-laki/Perempuan)</label>
                            <select id="gender" name="gender" class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm" required>
                                <option value="Laki-laki" <%= user.gender === 'Laki-laki' ? 'selected' : '' %>>Laki-laki</option>
                                <option value="Perempuan" <%= user.gender === 'Perempuan' ? 'selected' : '' %>>Perempuan</option>
                            </select>
                        </div>
                        <div class="flex items-center justify-between">
                            <button type="submit" class="bg-customBlue-base hover:bg-customBlue-deep text-white font-medium py-2 px-4 mt-6 rounded-lg transition-colors duration-300 ease-in-out">Simpan</button>
                        </div>
                    </form>
                    
                </div>
            </div>
        </div>
    </div>

    <%- include('footer') %>

</body>
</html>


